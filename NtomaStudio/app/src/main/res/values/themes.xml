<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme. Colours are supplied by the Compose Material 3 theme; this only needs
         to be a valid no-action-bar window for the Activity. -->
    <style name="Theme.Ntoma" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Ntoma.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">220</item>
        <item name="postSplashScreenTheme">@style/Theme.Ntoma</item>
    </style>
</resources>
